/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['t3.ftcdn.net', 'drive.google.com','media.gettyimages.com'], // Add all domains in one array
    },
};

export default nextConfig;
